#ifndef COPYFILE_THING_H_   /* Include guard */
#define COPYFILE_THING_H_

/*
returns -1 if it failed, returns 0 if it worked
*/
int copy_file(const char *to, const char *from);

#endif // COPYFILE_THING_H_